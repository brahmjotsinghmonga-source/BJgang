import { Crown, Shield } from "lucide-react";
import DiscordIcon from "./DiscordIcon";
import bjAvatar from "@/assets/bj-avatar.jpg";
import farofaAvatar from "@/assets/farofa-avatar.jpg";
import fulvoAvatar from "@/assets/fulvo-avatar.jpg";
import tengAvatar from "@/assets/teng-avatar.png";

const staffMembers = [
  {
    name: "BJ",
    role: "Owner",
    roles: ["Admin"],
    roleColor: "#FFD700",
    icon: Crown,
    bio: "Founder and owner of BJgang. The mastermind behind it all!",
    avatarImage: bjAvatar
  },
  {
    name: "Fulvo",
    role: "Moderator",
    roles: ["BJG-Staff Member", "Seriously Brainrotted", "BJG-Server Booster"],
    roleColor: "#E91E63",
    icon: Shield,
    bio: "Keeping the server clean and the vibes immaculate. Don't break the rules!",
    avatarImage: fulvoAvatar
  },
  {
    name: "Farofa",
    role: "Moderator", 
    roles: ["BJG-Staff Member", "BJG-Coder", "Cute"],
    roleColor: "#E91E63",
    icon: Shield,
    bio: "Coder and mod extraordinaire. Here to help with anything!",
    avatarImage: farofaAvatar
  },
  {
    name: "Teng",
    role: "Moderator", 
    roles: ["BJG-Staff Member", "BJG-Coder", "Cute"],
    roleColor: "#E91E63",
    icon: Shield,
    bio: "The adorable addition to the team. Don't let the looks fool you!",
    avatarImage: tengAvatar
  }
];

const StaffSection = () => {
  return (
    <section className="py-20 md:py-28 relative overflow-hidden">
      {/* Background decoration */}
      <div className="absolute top-0 right-0 w-96 h-96 bg-discord-fuchsia/10 rounded-full blur-3xl" />
      <div className="absolute bottom-0 left-0 w-96 h-96 bg-primary/10 rounded-full blur-3xl" />
      
      <div className="container mx-auto px-6 relative z-10">
        <div className="text-center mb-12">
          <h2 className="animate-slide-up opacity-0 text-4xl md:text-5xl font-black mb-4">
            Meet the <span className="text-gradient">Staff</span>
          </h2>
          <p className="animate-slide-up opacity-0 delay-100 text-muted-foreground text-lg max-w-2xl mx-auto">
            The awesome people keeping BJgang running. Feel free to reach out!
          </p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 max-w-4xl mx-auto">
          {staffMembers.map((member, index) => {
            const RoleIcon = member.icon;
            return (
              <div
                key={member.name}
                className="animate-slide-up opacity-0 group"
                style={{ animationDelay: `${index * 100 + 200}ms` }}
              >
                <div className="relative bg-card/60 backdrop-blur-sm border border-white/10 rounded-2xl p-6 h-full hover:border-white/20 transition-all duration-300 hover:scale-[1.02] hover:-translate-y-1">
                  {/* Status indicator */}
                  <div className="absolute top-4 right-4">
                    <div className="w-3 h-3 bg-discord-green rounded-full animate-pulse" />
                  </div>
                  
                  {/* Avatar */}
                  <div className="relative mx-auto w-20 h-20 mb-4">
                    <img 
                      src={member.avatarImage} 
                      alt={member.name}
                      className="w-full h-full rounded-full object-cover"
                      style={{ 
                        boxShadow: `0 0 30px -5px ${member.roleColor}40`
                      }}
                    />
                    {/* Discord badge */}
                    <div className="absolute -bottom-1 -right-1 p-1.5 rounded-full bg-card border border-white/10">
                      <DiscordIcon className="w-4 h-4 text-primary" />
                    </div>
                  </div>

                  {/* Info */}
                  <div className="text-center">
                    <h3 className="font-bold text-lg mb-1">{member.name}</h3>
                    <div 
                      className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-sm font-medium mb-2"
                      style={{ 
                        backgroundColor: `${member.roleColor}20`,
                        color: member.roleColor
                      }}
                    >
                      <RoleIcon className="w-3.5 h-3.5" />
                      {member.role}
                    </div>
                    {/* Additional roles */}
                    <div className="flex flex-wrap justify-center gap-1.5 mb-3">
                      {member.roles.map((role) => (
                        <span 
                          key={role}
                          className="px-2 py-0.5 rounded-full text-xs bg-white/5 text-muted-foreground border border-white/10"
                        >
                          {role}
                        </span>
                      ))}
                    </div>
                    <p className="text-sm text-muted-foreground leading-relaxed">
                      {member.bio}
                    </p>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default StaffSection;
