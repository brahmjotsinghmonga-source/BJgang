import { Button } from "@/components/ui/button";
import DiscordIcon from "./DiscordIcon";
import BlobBackground from "./BlobBackground";
import bjgangLogo from "@/assets/bjgang-logo.jpg";

const INVITE_LINK = "https://discord.gg/WbMFKTXTt";

const HeroSection = () => {
  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-hero">
      <BlobBackground />
      
      <div className="relative z-10 container mx-auto px-6 text-center">
        {/* Server Icon */}
        <div className="animate-slide-up opacity-0 mb-8">
          <div className="inline-flex items-center justify-center w-24 h-24 md:w-32 md:h-32 rounded-3xl overflow-hidden card-shadow animate-float">
            <img src={bjgangLogo} alt="BJgang Logo" className="w-full h-full object-cover" />
          </div>
        </div>

        {/* Main Heading */}
        <h1 className="animate-slide-up opacity-0 delay-100 text-5xl md:text-7xl lg:text-8xl font-black mb-6 tracking-tight">
          <span className="text-foreground">Welcome to</span>
          <br />
          <span className="text-gradient">BJgang</span>
        </h1>

        {/* Subheading */}
        <p className="animate-slide-up opacity-0 delay-200 text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto mb-10 leading-relaxed">
          Join the ultimate gaming community. Connect with friends, share epic moments, 
          and be part of something legendary.
        </p>

        {/* CTA Buttons */}
        <div className="animate-slide-up opacity-0 delay-300 flex flex-col sm:flex-row gap-4 justify-center items-center">
          <Button 
            variant="discord" 
            size="xl" 
            asChild
          >
            <a href={INVITE_LINK} target="_blank" rel="noopener noreferrer">
              <DiscordIcon className="w-6 h-6" />
              Join the Server
            </a>
          </Button>
          
          <Button 
            variant="outline" 
            size="lg"
            onClick={() => document.getElementById('features')?.scrollIntoView({ behavior: 'smooth' })}
          >
            Learn More
          </Button>
        </div>

        {/* Stats */}
        <div className="animate-slide-up opacity-0 delay-400 mt-16 flex flex-wrap justify-center gap-8 md:gap-16">
          <div className="text-center">
            <div className="text-3xl md:text-4xl font-black text-discord-green">24/7</div>
            <div className="text-sm text-muted-foreground uppercase tracking-wide">Active</div>
          </div>
          <div className="text-center">
            <div className="text-3xl md:text-4xl font-black text-discord-fuchsia">∞</div>
            <div className="text-sm text-muted-foreground uppercase tracking-wide">Fun</div>
          </div>
        </div>
      </div>

      {/* Scroll indicator */}
      <div className="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
        <div className="w-6 h-10 border-2 border-muted-foreground/50 rounded-full flex justify-center pt-2">
          <div className="w-1.5 h-3 bg-muted-foreground/50 rounded-full animate-pulse" />
        </div>
      </div>
    </section>
  );
};

export default HeroSection;
